---
layout: post
title: "Micro-Doppler Based Human-Robot Classification Using Ensemble and Deep Learning Approaches"
date: 2017-12-01 01:15:48
categories: arXiv_CV
tags: arXiv_CV CNN Classification Deep_Learning Recognition
author: Sherif Abdulatif, Qian Wei, Fady Aziz, Bernhard Kleiner, Urs Schneider
mathjax: true
---

* content
{:toc}

##### Abstract
Radar sensors can be used for analyzing the induced frequency shifts due to micro motions in both range and velocity dimensions identified as micro-Doppler ($\boldsymbol{\mu}$-D) and micro-Range ($\boldsymbol{\mu}$-R) respectively. Different moving targets will have unique $\boldsymbol{\mu}$-D and $\boldsymbol{\mu}$-R signatures that can be used for target classification. Such classification can be used in numerous fields such as gait recognition, safety and surveillance. In this paper, a \unit[25]{GHz} FMCW Single Input Single Output (SISO) radar is used in industrial safety for real-time human-robot identification. Due to the real-time constraint, joint Range-Doppler (R-D) maps are directly analyzed for our classification problem. Furthermore, a comparison between the conventional classical learning approaches with handcrafted extracted features, ensemble classifiers and deep learning approaches is presented. For ensemble classifiers, a restructured range and velocity profiles are passed directly to ensemble trees such as gradient boosting and random forest without feature extraction. Finally, a Deep Convolutional Neural Network (DCNN) is used and raw R-D images are directly fed to the constructed network. DCNN shows a superior performance of 99\% accuracy in identifying humans from robots on a single R-D map.

##### Abstract (translated by Google)
雷达传感器可以用来分析微小的多普勒（$ \ boldsymbol {\ mu} $  -  D）和微距（$ \ boldsymbol {\ mu} $ -R）。不同的移动目标将具有可用于目标分类的独特的$ \ boldsymbol {\ mu} $  -  D和$ \ boldsymbol {\ mu} $  -  R签名。这样的分类可以用于许多领域，例如步态识别，安全和监视。在本文中，一个单元[25] {GHz} FMCW单输入单输出（SISO）雷达用于工业安全实时人机识别。由于实时约束，直接分析联合距离 - 多普勒（R-D）地图是为了我们的分类问题。此外，将传统的经典学习方法与手工提取的特征，集成分类器和深度学习方法进行比较。对于集合分类器，重构的范围和速度分布直接传递到诸如梯度提升和随机森林的集合树，而不需要特征提取。最后，使用深度卷积神经网络（DCNN），并将原始R-D图像直接馈送到所构建的网络。 DCNN在单个R-D图上显示出机器人识别人类99％的高精度。

##### URL
[https://arxiv.org/abs/1711.09177](https://arxiv.org/abs/1711.09177)

##### PDF
[https://arxiv.org/pdf/1711.09177](https://arxiv.org/pdf/1711.09177)

